{% extends 'BaseEmail.twig' %}
{% from 'partials/button.twig' import button as button %}

{% block body %}{% apply spaceless %}
    {{ bodyStart|raw }}

    {{ parStart|raw }}
        Hi,
    {{ parEnd|raw }}

    {{ parSpacer|raw }}

    {{ parStart|raw }}
        You have been invited to join {{ orgName }}.
        Please log in to view this invite.
    {{ parEnd|raw }}

    {{ parSpacer|raw }}

    {{ button(uiUrl,'Log in') }}

    {{ parSpacer|raw }}

    {{ bodyEnd|raw }}
{% endapply %}{% endblock %}
