import { FC } from 'react';
import ActionPermissionSection from './ActionPermissionSection';

const DocumentCharSetActionPermission: FC = () => {
    return (
        <ActionPermissionSection
            title="Reads document character set"
            subtitle="Reads document character set."
        />
    );
};

export default DocumentCharSetActionPermission;
