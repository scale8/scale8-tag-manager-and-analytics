import { FC } from 'react';
import ActionPermissionSection from './ActionPermissionSection';

const PageTitleActionPermission: FC = () => {
    return (
        <ActionPermissionSection title="Reads document title" subtitle="Reads document title." />
    );
};

export default PageTitleActionPermission;
